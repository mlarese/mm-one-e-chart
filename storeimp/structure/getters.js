export const getters = {
  structureId: state => state.structureConfig.structureId,
  portalId: state => state.structureConfig.portalId,
  userLanguageCode: state => state.structureConfig.userLanguageCode
}
